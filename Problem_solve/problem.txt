Question 1: Generics & Utility Types (Omit, generic.ts)
__________________________________________________________
---Complete the generic function definition below so that
 it accepts an object without an id and returns the full 
 object with an id.

 interface User {
  id: number;
  name: string;
  email: string;
}

// 1. Define UserInput type using the Omit utility type to remove 'id'
type UserInput = // Write your code here

// 2. Complete the generic function signature
function createRecord<T>(input: UserInput): User {
  return {
    id: Math.floor(Math.random() * 1000),
    ...input
  };
}

Task: Write the code for UserInput using Omit, and explain 
how a utility type like Omit helps you prevent unwanted 
variables or properties in your code.




Question 2: Maps and Enums 
_____________________________________________________
enum UserRole {
  Admin = "ADMIN",
  Developer = "DEVELOPER",
  Guest = "GUEST"
}

// Key is the userId (number), Value is the UserRole
const activeSessions = new Map<number, UserRole>();

activeSessions.set(101, UserRole.Admin);
activeSessions.set(102, UserRole.Developer);


Task: Write the TypeScript code to check if userId 102 exists
 in the activeSessions map.
 If it exists, retrieve its value and print it to the console.



 Q3 Write an interface for a backend API response:

 {
    "success": true,
    "message": "User created successfully",
    "data": {
        "id": 1,
        "name": "Tarun",
        "email": "tarun@gmail.com"
    }
}